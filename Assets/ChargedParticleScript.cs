using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChargedParticleScript : MonoBehaviour
{
    public float ChargeOfThisParticle;  //user must specify negative charges (e.g. ChargeOfThisParticle = -2)

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
